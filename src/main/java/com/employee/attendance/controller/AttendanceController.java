package com.employee.attendance.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class AttendanceController {
	
	
	
	/*
	 * @Autowired private MessageConsumer messageConsumer;
	 * 
	 * @Autowired private ObjectMapper objectMapper;
	 * 
	 * @PostMapping("/addEmployee") public String
	 * addEmployee(@RequestParam("employeeName") String employeeName ) {
	 * 
	 * employeeService.addEmployee(employeeName ); AttendanceData data =
	 * messageConsumer.listen("attendance"); System.out.println(data);
	 * System.out.println(data.getEmployeeId());
	 * System.out.println(data.getHours()); System.out.println(data.getName());
	 * //messageProducer.sendMessage("my_topic", "I am all right"); return
	 * "added employee"; }
	 * 
	 * @GetMapping("/findById") public Optional<Employee> findEmployee(@RequestParam
	 * long employeeId) { Optional<Employee> employee
	 * =employeeService.findEmployee(employeeId); return employee;
	 * 
	 * }
	 */
}
